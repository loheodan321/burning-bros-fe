"use client";

import React from "react";
import { Layout } from "antd";
const { Header } = Layout;

const NavBarComponent = () => {
  return <Header style={{ backgroundColor: "white" }}>NavBarComponent</Header>;
};

export default NavBarComponent;
